<ul class="pagination pull-right">
    <% if (!isFirstPage) { %>
        <a class="btn btn-success " title="上一页" href="?p=<%= page-1 %>">上一页</a></li>
        <% } %>
            <% if (!isLastPage) { %>
                <a class="btn btn-success " title="下一页" href="?p=<%= page+1 %>">下一页</a></li>
                <% } %>

</ul>